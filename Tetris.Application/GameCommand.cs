namespace Tetris.Application
{
    public enum GameCommand
    {
        nothing = 0,
        moveLeft,
        moveRight,
        moveDown,
        rotateLeft,
        rotateRight,
        hold,
        hardDrop,
    }
}
